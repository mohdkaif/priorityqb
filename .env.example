# Mail (contact form) – same as Laravel-style
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="Priority QB Services"

# Where to receive contact form submissions
CONTACT_EMAIL=your-email@example.com

# --- Gmail: use App Password, not your normal password ---
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your@gmail.com
# MAIL_PASSWORD=your-16-char-app-password
# Create app password: Google Account → Security → 2-Step Verification → App passwords

# --- Dev: skip sending email, log to console (form still returns success) ---
# MAIL_DEV_SKIP_SEND=true
